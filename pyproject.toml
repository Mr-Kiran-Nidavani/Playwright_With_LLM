[project]
name = "playwright-with-llm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langsmith[claude-agent-sdk]>=0.7.22",
]
